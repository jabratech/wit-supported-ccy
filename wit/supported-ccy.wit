interface supported-ccy-handler {
    use jabratech:common/types.{response};
    use types.{supported-ccy-response};

    get:  func(id: option<s16>, ticker: option<string>, name: option<string>) -> result<supported-ccy-response, response>;
    
  }